# 預設行為：自動判斷文字檔案，並在簽出時轉為原生行尾 (Windows 為 CRLF, Linux 為 LF)，提交時轉為 LF
* text=auto

# 強制所有文字檔案在存儲庫中以 LF 儲存
*.txt text eol=lf
*.md text eol=lf
*.js text eol=lf
*.py text eol=lf
*.html text eol=lf
*.css text eol=lf

# 強制某些特定檔案始終使用 LF (例如 shell 腳本)
*.sh text eol=lf

# 排除二進位檔案，避免被轉換
*.png binary
*.jpg binary
*.gif binary